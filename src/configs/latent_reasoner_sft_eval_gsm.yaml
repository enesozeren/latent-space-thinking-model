# Model configuration
model:
  base_model_name_or_path: "/dss/dssmcmlfs01/pr74ze/pr74ze-dss-0001/ra32qov2/latent_reasoner_storage/outputs/latent_reasoner_1p5b_sft/20250703_085301/hf_checkpoints/epoch=05"
  is_latent_reasoner: True  # Set to true for latent reasoner evaluation
  num_latent_steps: 30  # Number of latent steps

# Dataset configuration
dataset:
  dataset: "openai/gsm8k"  # choices: ["openai/gsm8k", "HuggingFaceH4/MATH-500"]
  split: "test"            # choices: ["test", "train"]
  num_examples: null       # null means use all examples, or specify a number

# Generation parameters
generation:
  temperature: 0.2
  top_p: 0.95
  max_length: 2048
  batch_size: 4

# Output configuration
logs:
  output_dir: "outputs"
  wandb_project: "latent_reasoner_eval"
  wandb_run_name: latent_reasoner_1p5b_sft-epoch05_30LatentSteps_gsm8k_eval
  no_wandb: false

# Reproducibility
seed: 42