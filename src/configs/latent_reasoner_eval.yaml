# Model configuration
model_name_or_path: "outputs/latent_reasoner_sft_rl/checkpoint-200"
is_latent_reasoner: true  # Set to true for latent reasoner evaluation
num_latent_steps: 10  # Number of latent steps

# Dataset configuration
dataset: "openai/gsm8k"  # choices: ["openai/gsm8k", "HuggingFaceH4/MATH-500"]
split: "test"            # choices: ["test", "train"]
num_examples: null       # null means use all examples, or specify a number

# Generation parameters
temperature: 0.2
top_p: 0.95
max_length: 1024
batch_size: 8

# Output configuration
output_dir: "outputs"
wandb_project: "latent_reasoner_eval"
wandb_run_name: null     # null means auto-generate, or specify a string
no_wandb: false

# Reproducibility
seed: 42